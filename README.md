# Tweets timeline
### Instruction to start the project:

* Run `npm install` and then `nodemon`
* Go to the client folder, run `npm install` and then `ng serve`
* Go to the `http://localhost:4200`

### Heroku deploy:
* https://twittertt.herokuapp.com/
